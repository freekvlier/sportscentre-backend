server:
    port: 8081

spring:
  kafka:
    bootstrap-servers: my-cluster-kafka-bootstrap:9092

  application:
    name: user-service
  data:
    mongodb:
      uri: mongodb://user:user@mongo-user:27017
      database: user